export type Episode = {
  id: string;
  title: string;
  description: string;
  author: string;
  imageUrl: string;
  publishedAt: string;
  duration: string;
  audioUrl: string;
};

export type Podcast = {
  id: string;
  title: string;
  imageUrl: string;
  author: string;
};
